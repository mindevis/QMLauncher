# 🤝 Contributing to QMLauncher

Спасибо за интерес к разработке QMLauncher! Этот документ описывает процесс разработки и правила contribution.

## 📋 Правила разработки

### 🔄 Conventional Commits

Все коммиты должны следовать формату [Conventional Commits](https://conventionalcommits.org/):

```bash
type(scope): description

[optional body]

[optional footer]
```

#### Допустимые типы:
- `feat` - новая функциональность
- `fix` - исправление багов
- `docs` - изменения документации
- `style` - форматирование кода
- `refactor` - рефакторинг кода
- `test` - добавление/изменение тестов
- `chore` - обслуживание (обновление зависимостей и т.д.)
- `ci` - изменения CI/CD
- `build` - изменения сборки
- `perf` - улучшения производительности

#### Примеры:
```bash
feat: add interactive mode for launcher
fix: resolve crash when server is unreachable
docs: update installation instructions
chore: update dependencies
ci: add commitlint validation
```

### 🧪 Автоматический CI/CD

#### При push/merge в `main`:
1. **Проверка защиты ветки** - только мерджи из `dev` разрешены
2. **Валидация коммитов** - conventional commits
3. **Линтинг** - `golangci-lint`
4. **Тесты** - `go test` с покрытием (минимум 80%)
5. **Semantic Release** - автоматическое определение версии
6. **Создание тега** - `v1.2.3` на основе conventional commits
7. **Создание релиза** - с changelog и бинарниками
8. **Cross-platform сборка** - Linux, macOS, Windows с иконками

#### При push в `dev`:
1. **Тесты** - `go test` с race detection

## 🌳 Работа с ветками (Git Flow)

Проект использует [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/) для управления ветками. Это обеспечивает порядок в разработке и стабильность релизов.

### Основные ветки:
- **`main`** - production ветка, содержит стабильные релизы 🚀
- **`dev`** - ветка разработки, содержит готовые к релизу изменения 🧪

### Вспомогательные ветки:
- **`feature/*`** - ветки для разработки новых функций (например: `feature/bubbletea`)
- **`release/*`** - релизные ветки для подготовки релизов
- **`hotfix/*`** - ветки для срочных исправлений в production
- **`bugfix/*`** - ветки для исправления багов

---

## 📋 Ежедневные команды Git Flow

### 🔍 Просмотр веток
```bash
# Посмотреть все локальные ветки
git branch

# Посмотреть все ветки (локальные + remote)
git branch -a

# Посмотреть текущую ветку и статус
git status

# Посмотреть последние коммиты в виде графа
git log --oneline --graph --all -10
```

### 🔄 Переключение между ветками
```bash
# Переключиться на существующую ветку
git checkout dev
git checkout main
git checkout feature/my-feature

# Создать и переключиться на новую ветку (обычный способ)
git checkout -b feature/new-feature

# Вернуться на предыдущую ветку
git checkout -
```

### 🌟 Git Flow команды

#### Feature ветки (для новых функций):
```bash
# Начать разработку новой фичи
git flow feature start my-awesome-feature
# Создает и переключается на feature/my-awesome-feature

# Закончить разработку (автоматически сольет в dev)
git flow feature finish my-awesome-feature

# Опубликовать feature ветку (для командной работы)
git flow feature publish my-awesome-feature

# Получить опубликованную feature ветку
git flow feature pull origin my-awesome-feature
```

#### Release ветки (для подготовки релиза):
```bash
# Начать подготовку релиза
git flow release start v1.2.3
# Создает release/v1.2.3 от dev

# Закончить релиз (сольет в main и dev, создаст тег)
git flow release finish v1.2.3

# Опубликовать release ветку
git flow release publish v1.2.3
```

#### Hotfix ветки (для срочных исправлений):
```bash
# Начать срочное исправление
git flow hotfix start critical-fix
# Создает hotfix/critical-fix от main

# Закончить hotfix (сольет в main и dev, создаст тег)
git flow hotfix finish critical-fix
```

---

## 🚀 Полный рабочий процесс разработчика

### Шаг 1: Подготовка (один раз)
```bash
# Клонировать репозиторий
git clone https://github.com/mindevis/QMLauncher.git
cd QMLauncher

# Настроить upstream (если работаете с форком)
git remote add upstream https://github.com/mindevis/QMLauncher.git

# Инициализировать Git Flow (уже настроено в проекте)
git flow init  # Использовать настройки по умолчанию
```

### Шаг 2: Начало работы над задачей
```bash
# Убедиться, что находимся в dev и он обновлен
git checkout dev
git pull origin dev

# Начать новую feature
git flow feature start add-user-authentication

# Или переключиться на существующую
git checkout feature/add-user-authentication
```

### Шаг 3: Разработка
```bash
# Внести изменения
# Написать код, тесты, обновить документацию

# Посмотреть изменения
git status
git diff

# Добавить файлы
git add .

# Сделать коммит (использовать conventional commits)
git commit -m "feat: add user authentication with JWT tokens"

# Push изменений (если работаете в команде)
git push origin feature/add-user-authentication
```

### Шаг 4: Code Review и Merge
```bash
# Создать Pull Request в GitHub (из feature/* в dev)
# После approval и прохождения CI:

# Закончить feature (локально)
git flow feature finish add-user-authentication

# Или если ветка опубликована:
git checkout dev
git merge feature/add-user-authentication
git branch -d feature/add-user-authentication
```

### Шаг 5: Релиз
```bash
# Когда dev готов к релизу (только maintainer):
git checkout dev
git pull origin dev

# Создать release ветку
git flow release start v1.2.3

# Протестировать релиз
# Исправить баги если нужно

# Закончить релиз (создаст тег и сольет в main)
git flow release finish v1.2.3

# Push изменений
git push origin main
git push origin dev
git push --tags
```

---

## 🛠️ Расширенные команды

### Работа с remote ветками
```bash
# Синхронизировать с remote
git fetch origin

# Удалить merged ветки
git branch -d feature/old-feature

# Удалить remote ветку
git push origin --delete feature/old-feature

# Переименовать ветку
git branch -m feature/old-name feature/new-name
```

### Разрешение конфликтов
```bash
# При мердже возник конфликт
git status  # посмотреть конфликтующие файлы

# Отредактировать файлы, разрешить конфликты
# Затем:
git add <resolved-files>
git commit

# Или отменить merge
git merge --abort
```

### Cherry-pick коммитов
```bash
# Перенести коммит из другой ветки
git cherry-pick <commit-hash>

# Перенести несколько коммитов
git cherry-pick <start-commit>..<end-commit>
```

### Работа с историей
```bash
# Посмотреть историю ветки
git log --oneline feature/my-branch

# Сравнить ветки
git diff dev..feature/my-feature

# Посмотреть кто что менял
git blame <file>
```

---

## ⚠️ Важные правила

### ❌ Чего НЕ делать:
- **Не пушить напрямую в `main`** - только через merge из `dev`
- **Не создавать PR в `main`** - только в `dev`
- **Не мерджить `main` в feature ветки** - обновляйтесь из `dev`
- **Не удаляйте ветки с незавершенной работой**

### ✅ Лучшие практики:
- **Регулярно синхронизируйтесь** с `dev`: `git pull origin dev`
- **Делайте мелкие коммиты** с понятными сообщениями
- **Тестируйте код** перед push
- **Используйте conventional commits** для автоматических релизов
- **Удаляйте merged feature ветки** после завершения

### 🔍 Troubleshooting

#### "Branch already exists"
```bash
# Проверить существующие ветки
git branch -a

# Удалить локальную ветку
git branch -D feature/existing-branch

# Удалить remote ветку
git push origin --delete feature/existing-branch
```

#### "Cannot delete branch" (текущая ветка)
```bash
# Переключиться на другую ветку
git checkout dev
git branch -D feature/current-branch
```

#### Забыли переключиться на feature ветку
```bash
# Если сделали коммиты не в ту ветку
git log --oneline -5  # найти хэш последнего правильного коммита
git reset --hard <correct-commit-hash>
git checkout -b feature/correct-branch
# Повторить работу
```

---

## 📚 Дополнительные ресурсы

- **[Подробный Git Workflow гайд](docs/git-workflow.md)** - полное руководство по веткам
- [Официальная документация Git Flow](https://nvie.com/posts/a-successful-git-branching-model/)
- [Conventional Commits](https://conventionalcommits.org/)
- [Git Flow Cheat Sheet](https://danielkummer.github.io/git-flow-cheatsheet/)
- [Interactive Git Tutorial](https://learngitbranching.js.org/)

## 🚀 Разработка

### 📋 Предварительные требования

```bash
# Go 1.21+
go version

# Node.js для commitlint (опционально)
node --version

# ImageMagick для работы с иконками
convert -version
```

### 🛠️ Настройка

```bash
# Клонировать репозиторий
git clone https://github.com/mindevis/QMLauncher.git
cd QMLauncher

# Установить зависимости
go mod download

# Установить commitlint (опционально)
npm install -g @commitlint/cli @commitlint/config-conventional
```

### 🧪 Локальное тестирование

```bash
# Проверить conventional commits
.github/workflows/validate-commits.sh

# Протестировать сборку как в CI/CD
.github/workflows/test-local-build.sh

# Запустить тесты
go test ./...

# Проверить линтинг
golangci-lint run
```

### 📝 Создание изменений

```bash
# Создать feature ветку с помощью Git Flow
git flow feature start my-awesome-feature

# Внести изменения
# Добавить тесты
# Обновить документацию

# Сделать коммит в conventional формате
git commit -m "feat: add awesome new feature"

# Закончить feature (автоматически сольет в dev)
git flow feature finish my-awesome-feature

# Или push feature ветки для code review
git push origin feature/my-awesome-feature
# Создать Pull Request из feature/my-awesome-feature в dev
# ⚠️ Pull request'ы разрешены ТОЛЬКО в ветку dev!
```

## 🛡️ Правила защиты веток

### Ветка `main` (Production)
- **Запрещены прямые пуши** - только мерджи из `dev`
- **Запрещены pull request'ы** - изменения попадают только через merge из `dev`
- **Требуется CI** - все проверки должны проходить при merge
- **Conventional commits** - обязательны
- **Code coverage** - минимум 80%

### Ветка `dev` (Development)
- **Разрешены пуши** от разработчиков
- **Требуются тесты** - должны проходить
- **Code review** - рекомендуется для крупных изменений

### Настройка Branch Protection (GitHub)

📖 **Подробные инструкции**: [.github/branch-protection-setup.md](.github/branch-protection-setup.md)

Кратко:
- **Веб-интерфейс**: Settings → Branches → Add rule для `main`
- **Требования**: PR reviews, status checks (CI), include admins
- **Ограничения**: только мерджи из `dev`

## 🎯 Процесс релиза

### Автоматический релиз
1. **Push conventional commit в `main`**
2. **CI проходит все проверки**
3. **Semantic Release определяет версию** (patch/minor/major)
4. **Создается git tag** `v1.2.3`
5. **Создается GitHub Release** с changelog
6. **Запускается cross-platform сборка**
7. **Бинарники загружаются в релиз**

### Ручной релиз (при необходимости)
```bash
# Создать conventional commit для релиза
git commit -m "chore: prepare release"

# Push в main
git push origin main
```

## 📦 Работа с иконками

```bash
# Проверить иконки
make check-icon

# Конвертировать в другие форматы
make convert-icons

# Установить зависимости для конвертации
# Dependencies are installed automatically in CI/CD
```

## 🐛 Отладка CI/CD

### Просмотр логов
- **Actions** → выберите workflow → кликните на job
- **Artifacts** → скачать логи и бинарники

### Локальная симуляция
```bash
# Симулировать GitHub Actions
.github/workflows/test-local-build.sh

# Проверить conventional commits
.github/workflows/validate-commits.sh
```

## 📚 Документация

### Обновление README
- Добавляйте новые возможности в раздел "Возможности"
- Обновляйте примеры использования
- Добавляйте новые команды в справку

### Code Documentation
```go
// Package comment
package mypackage

// Function comment
func MyFunction() {
    // Implementation
}
```

## 🙋 Вопросы

При возникновении вопросов:
1. Проверьте этот документ
2. Посмотрите существующие Issues
3. Создайте новый Issue с подробным описанием

**Спасибо за contribution в QMLauncher! 🚀**