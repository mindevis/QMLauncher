# 🤝 Contributing to QMLauncher

Спасибо за интерес к разработке QMLauncher! Этот документ описывает процесс разработки и правила contribution.

## 📋 Правила разработки

### 🔄 Conventional Commits

Все коммиты должны следовать формату [Conventional Commits](https://conventionalcommits.org/):

```bash
type(scope): description

[optional body]

[optional footer]
```

#### Допустимые типы:
- `feat` - новая функциональность
- `fix` - исправление багов
- `docs` - изменения документации
- `style` - форматирование кода
- `refactor` - рефакторинг кода
- `test` - добавление/изменение тестов
- `chore` - обслуживание (обновление зависимостей и т.д.)
- `ci` - изменения CI/CD
- `build` - изменения сборки
- `perf` - улучшения производительности

#### Примеры:
```bash
feat: add interactive mode for launcher
fix: resolve crash when server is unreachable
docs: update installation instructions
chore: update dependencies
ci: add commitlint validation
```

### 🧪 Автоматический CI/CD

#### При push в `main` или `develop`:
1. **Линтинг** - `golangci-lint`
2. **Тесты** - `go test` с покрытием
3. **Сборка** - проверка компиляции для всех платформ
4. **Conventional Commits** - валидация формата коммитов

#### При merge в `main`:
1. **Semantic Release** - автоматическое определение версии
2. **Создание тега** - `v1.2.3` на основе conventional commits
3. **Создание релиза** - с changelog и бинарниками
4. **Cross-platform сборка** - Linux, macOS, Windows с иконками

## 🚀 Разработка

### 📋 Предварительные требования

```bash
# Go 1.21+
go version

# Node.js для commitlint (опционально)
node --version

# ImageMagick для работы с иконками
convert -version
```

### 🛠️ Настройка

```bash
# Клонировать репозиторий
git clone https://github.com/mindevis/QMLauncher.git
cd QMLauncher

# Установить зависимости
go mod download

# Установить commitlint (опционально)
npm install -g @commitlint/cli @commitlint/config-conventional
```

### 🧪 Локальное тестирование

```bash
# Проверить conventional commits
.github/workflows/validate-commits.sh

# Протестировать сборку как в CI/CD
.github/workflows/test-local-build.sh

# Запустить тесты
go test ./...

# Проверить линтинг
golangci-lint run
```

### 📝 Создание изменений

```bash
# Создать ветку для feature
git checkout -b feature/my-feature

# Внести изменения
# Добавить тесты
# Обновить документацию

# Сделать коммит в conventional формате
git commit -m "feat: add awesome new feature"

# Push и создать PR
git push origin feature/my-feature
```

## 🎯 Процесс релиза

### Автоматический релиз
1. **Push conventional commit в `main`**
2. **CI проходит все проверки**
3. **Semantic Release определяет версию** (patch/minor/major)
4. **Создается git tag** `v1.2.3`
5. **Создается GitHub Release** с changelog
6. **Запускается cross-platform сборка**
7. **Бинарники загружаются в релиз**

### Ручной релиз (при необходимости)
```bash
# Создать conventional commit для релиза
git commit -m "chore: prepare release"

# Push в main
git push origin main
```

## 📦 Работа с иконками

```bash
# Проверить иконки
make check-icon

# Конвертировать в другие форматы
make convert-icons

# Установить зависимости для конвертации
./assets/install-deps.sh
```

## 🐛 Отладка CI/CD

### Просмотр логов
- **Actions** → выберите workflow → кликните на job
- **Artifacts** → скачать логи и бинарники

### Локальная симуляция
```bash
# Симулировать GitHub Actions
.github/workflows/test-local-build.sh

# Проверить conventional commits
.github/workflows/validate-commits.sh
```

## 📚 Документация

### Обновление README
- Добавляйте новые возможности в раздел "Возможности"
- Обновляйте примеры использования
- Добавляйте новые команды в справку

### Code Documentation
```go
// Package comment
package mypackage

// Function comment
func MyFunction() {
    // Implementation
}
```

## 🙋 Вопросы

При возникновении вопросов:
1. Проверьте этот документ
2. Посмотрите существующие Issues
3. Создайте новый Issue с подробным описанием

**Спасибо за contribution в QMLauncher! 🚀**