# 🤝 Contributing to QMLauncher

Спасибо за интерес к разработке QMLauncher! Этот документ описывает процесс разработки и правила contribution.

## 📋 Правила разработки

### 🔄 Conventional Commits

Все коммиты должны следовать формату [Conventional Commits](https://conventionalcommits.org/):

```bash
type(scope): description

[optional body]

[optional footer]
```

#### Допустимые типы:
- `feat` - новая функциональность
- `fix` - исправление багов
- `docs` - изменения документации
- `style` - форматирование кода
- `refactor` - рефакторинг кода
- `test` - добавление/изменение тестов
- `chore` - обслуживание (обновление зависимостей и т.д.)
- `ci` - изменения CI/CD
- `build` - изменения сборки
- `perf` - улучшения производительности

#### Примеры:
```bash
feat: add interactive mode for launcher
fix: resolve crash when server is unreachable
docs: update installation instructions
chore: update dependencies
ci: add commitlint validation
```

### 🧪 Автоматический CI/CD

#### При push/merge в `main`:
1. **Проверка защиты ветки** - только мерджи из `dev` разрешены
2. **Валидация коммитов** - conventional commits
3. **Линтинг** - `golangci-lint`
4. **Тесты** - `go test` с покрытием (минимум 80%)
5. **Semantic Release** - автоматическое определение версии
6. **Создание тега** - `v1.2.3` на основе conventional commits
7. **Создание релиза** - с changelog и бинарниками
8. **Cross-platform сборка** - Linux, macOS, Windows с иконками

#### При push в `dev`:
1. **Тесты** - `go test` с race detection

## 🌳 Структура веток (Git Flow)

Проект использует [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/) для управления ветками:

### Основные ветки:
- **`main`** - production ветка, содержит стабильные релизы
- **`dev`** - ветка разработки, содержит готовые к релизу изменения

### Вспомогательные ветки:
- **`feature/*`** - ветки для разработки новых функций (например: `feature/bubbletea`)
- **`release/*`** - релизные ветки для подготовки релизов
- **`hotfix/*`** - ветки для срочных исправлений в production
- **`bugfix/*`** - ветки для исправления багов

### Рабочий процесс:
```bash
# Начать разработку новой фичи
git flow feature start my-feature

# Закончить разработку (автоматически сольет в dev)
git flow feature finish my-feature

# Создать релиз из dev
git flow release start v1.2.3
git flow release finish v1.2.3  # сольет в main и dev, создаст тег

# Срочное исправление в production
git flow hotfix start critical-fix
git flow hotfix finish critical-fix
```

## 🚀 Разработка

### 📋 Предварительные требования

```bash
# Go 1.21+
go version

# Node.js для commitlint (опционально)
node --version

# ImageMagick для работы с иконками
convert -version
```

### 🛠️ Настройка

```bash
# Клонировать репозиторий
git clone https://github.com/mindevis/QMLauncher.git
cd QMLauncher

# Установить зависимости
go mod download

# Установить commitlint (опционально)
npm install -g @commitlint/cli @commitlint/config-conventional
```

### 🧪 Локальное тестирование

```bash
# Проверить conventional commits
.github/workflows/validate-commits.sh

# Протестировать сборку как в CI/CD
.github/workflows/test-local-build.sh

# Запустить тесты
go test ./...

# Проверить линтинг
golangci-lint run
```

### 📝 Создание изменений

```bash
# Создать feature ветку с помощью Git Flow
git flow feature start my-awesome-feature

# Внести изменения
# Добавить тесты
# Обновить документацию

# Сделать коммит в conventional формате
git commit -m "feat: add awesome new feature"

# Закончить feature (автоматически сольет в dev)
git flow feature finish my-awesome-feature

# Или push feature ветки для code review
git push origin feature/my-awesome-feature
# Создать Pull Request из feature/my-awesome-feature в dev
```

## 🛡️ Правила защиты веток

### Ветка `main` (Production)
- **Запрещены прямые пуши** - только мерджи из `dev`
- **Требуется CI** - все проверки должны проходить
- **Conventional commits** - обязательны
- **Code coverage** - минимум 80%

### Ветка `dev` (Development)
- **Разрешены пуши** от разработчиков
- **Требуются тесты** - должны проходить
- **Code review** - рекомендуется для крупных изменений

### Настройка Branch Protection (GitHub)

📖 **Подробные инструкции**: [.github/branch-protection-setup.md](.github/branch-protection-setup.md)

Кратко:
- **Веб-интерфейс**: Settings → Branches → Add rule для `main`
- **Требования**: PR reviews, status checks (CI), include admins
- **Ограничения**: только мерджи из `dev`

## 🎯 Процесс релиза

### Автоматический релиз
1. **Push conventional commit в `main`**
2. **CI проходит все проверки**
3. **Semantic Release определяет версию** (patch/minor/major)
4. **Создается git tag** `v1.2.3`
5. **Создается GitHub Release** с changelog
6. **Запускается cross-platform сборка**
7. **Бинарники загружаются в релиз**

### Ручной релиз (при необходимости)
```bash
# Создать conventional commit для релиза
git commit -m "chore: prepare release"

# Push в main
git push origin main
```

## 📦 Работа с иконками

```bash
# Проверить иконки
make check-icon

# Конвертировать в другие форматы
make convert-icons

# Установить зависимости для конвертации
# Dependencies are installed automatically in CI/CD
```

## 🐛 Отладка CI/CD

### Просмотр логов
- **Actions** → выберите workflow → кликните на job
- **Artifacts** → скачать логи и бинарники

### Локальная симуляция
```bash
# Симулировать GitHub Actions
.github/workflows/test-local-build.sh

# Проверить conventional commits
.github/workflows/validate-commits.sh
```

## 📚 Документация

### Обновление README
- Добавляйте новые возможности в раздел "Возможности"
- Обновляйте примеры использования
- Добавляйте новые команды в справку

### Code Documentation
```go
// Package comment
package mypackage

// Function comment
func MyFunction() {
    // Implementation
}
```

## 🙋 Вопросы

При возникновении вопросов:
1. Проверьте этот документ
2. Посмотрите существующие Issues
3. Создайте новый Issue с подробным описанием

**Спасибо за contribution в QMLauncher! 🚀**