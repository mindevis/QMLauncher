name: release

on:
  release:
    types: [created]
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build cross-platform GUI and CLI binaries
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.24'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: cd frontend && npm ci

      - name: Build frontend
        run: cd frontend && npm run build

      - name: Create dist directory
        run: mkdir -p dist

      - name: Build GUI binaries for all platforms
        run: |
          # Linux GUI
          GOOS=linux GOARCH=amd64 go build -o dist/qmlauncher-gui-linux-amd64 .
          GOOS=linux GOARCH=arm64 go build -o dist/qmlauncher-gui-linux-arm64 .

          # Windows GUI
          GOOS=windows GOARCH=amd64 go build -o dist/qmlauncher-gui-windows-amd64.exe .
          GOOS=windows GOARCH=arm64 go build -o dist/qmlauncher-gui-windows-arm64.exe .

          # macOS GUI (Intel)
          GOOS=darwin GOARCH=amd64 go build -o dist/qmlauncher-gui-darwin-amd64 .

          # macOS GUI (Apple Silicon)
          GOOS=darwin GOARCH=arm64 go build -o dist/qmlauncher-gui-darwin-arm64 .

      - name: Build CLI binaries for all platforms
        run: |
          # Linux CLI
          GOOS=linux GOARCH=amd64 go build -tags cli -o dist/qmlauncher-cli-linux-amd64 .
          GOOS=linux GOARCH=arm64 go build -tags cli -o dist/qmlauncher-cli-linux-arm64 .

          # Windows CLI
          GOOS=windows GOARCH=amd64 go build -tags cli -o dist/qmlauncher-cli-windows-amd64.exe .
          GOOS=windows GOARCH=arm64 go build -tags cli -o dist/qmlauncher-cli-windows-arm64.exe .

          # macOS CLI (Intel)
          GOOS=darwin GOARCH=amd64 go build -tags cli -o dist/qmlauncher-cli-darwin-amd64 .

          # macOS CLI (Apple Silicon)
          GOOS=darwin GOARCH=arm64 go build -tags cli -o dist/qmlauncher-cli-darwin-arm64 .

      - name: List built binaries
        run: |
          echo "=== GUI Versions ==="
          ls -la dist/ | grep gui
          echo ""
          echo "=== CLI Versions ==="
          ls -la dist/ | grep cli

      - name: Get tag name
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            echo "tag=${{ github.event.release.tag_name }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Upload binaries to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ steps.tag.outputs.tag }}"
          echo "Uploading binaries for tag: $TAG_NAME"
          gh release upload --clobber "$TAG_NAME" dist/*